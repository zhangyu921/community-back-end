const PASSWORD_SALT = 'just salt'
const JWT_SECRET = 'secret'
const COOKIE_SESSION_KEY = ['What', 'is', 'this', 'things', '?']

module.exports = {
  PASSWORD_SALT,
  JWT_SECRET,
  COOKIE_SESSION_KEY
}